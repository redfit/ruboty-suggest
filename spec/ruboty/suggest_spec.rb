require 'spec_helper'

describe Ruboty::Suggest do
  it 'has a version number' do
    expect(Ruboty::Suggest::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
